/**
 * Глобальная функция для логирования сообщений.
 * 
 * @global
 * @param {...any} args - Аргументы, которые нужно залогировать. Поддерживаются любые типы.
 * @returns {void}
 * 
 * Если передать функции json-объект, то он будет выведен в лог полностью с распечаткой всех полей.
 */
function log(...args) {
  // Тело функции уже определено где-то в глобальном контексте.
}